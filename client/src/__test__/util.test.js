describe('Map values function', () => {
  // Begins a new test.
  test('Should return an array with the values squared.', () => {
    const input = 1;
    const output = 1;

    // This is where the magic really happens.
    // We create an expectation object with expect() that gets resolved with a "matcher". In this case, it is .toEqual()
    // There are a bunch of them, but some of basic ones will be demoed in this code base.
    expect(input).toEqual(output);
  });
});
